//
// Created by wieceslaw on 09.02.23.
//

#include <string>
#include <iostream>
#include <vector>
#include <cmath>

int main() {
    std::string line;
    std::vector<std::pair<std::string, int>> parts;
    while (std::cin >> line) {
        int len = line.length();
        if (line[0] != '0') {
            parts.push_back()
        }
        int i = 0;
        while (i < len && line[i] == '0') {
            i++;
        }
        int n = ;
        if (i == len) {
            nm =
        } else {

        }
    }
}
