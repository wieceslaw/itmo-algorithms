//
// Created by wieceslaw on 24.02.23.
//

#include <iostream>
#include <cstddef>

int main() {
    int n, k, p, c = 0;
    std::cin >> n >> k >> p;
    for (std::size_t i = 0; i < p; i++) {
        int tmp;
        std::cin >> tmp;
    }
    return 0;
}
